// TEMPLATE — copy this to:
//   arxiv Summariser/Services/Secrets.swift
// then fill in your keys. The real Secrets.swift is gitignored and must never
// be committed. (This template lives at the repo root with a .example extension
// so Xcode's synchronized group does not compile it.)

import Foundation

enum Secrets {
    /// Google Gemini API key (https://aistudio.google.com/apikey).
    /// Leave empty to keep using the on-device model.
    static let geminiAPIKey = ""
}
